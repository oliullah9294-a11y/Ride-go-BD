# Ride Go BD — Firebase-ready starter

Firebase project: ride-go-bd-54111
Android package: com.ridegobd.app
Firebase App ID: 1:343172039992:android:4eb2ce65f66653cf66adcd

Included:
- Firebase Core dependency
- Firebase Auth dependency (for Phone OTP)
- Cloud Firestore dependency
- Firebase Cloud Messaging dependency
- Android google-services.json placeholder
- firebase_options.dart placeholder

IMPORTANT:
The API key was intentionally redacted from this package because it was shared in chat. The Firebase Console can regenerate/restrict it. Do not publish unrestricted API keys.

This is source configuration, not a built APK. A Flutter/Android build environment is still required to produce/install the APK.
